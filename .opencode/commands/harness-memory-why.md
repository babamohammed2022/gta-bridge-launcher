---
description: Explain why memories activate for a scope and trigger
---

Ask for a scope path and lifecycle trigger if not already provided, then run `npx harness-memory memory:why --db .harness-memory/memory.sqlite --scope <scope> --trigger <trigger> --json` and summarize activated, suppressed, and conflicting memories.
