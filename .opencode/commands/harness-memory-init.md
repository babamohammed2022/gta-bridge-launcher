---
description: Initialize harness-memory in this project
---

Run `npx harness-memory init --db .harness-memory/memory.sqlite` in the current project root. If the project is already initialized, inspect and summarize the current setup instead of overwriting files.
